<!DOCTYPE html>
<html>
    <head>
        <title>Comment Section</title>
    </head>
    <body style="font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;">
        <form action="/login" mehtod="post"> <button type="submit"> Logout</button></form>
        <h2 style="text-align: center;"> Welcome <%= user.username %>!</h2> 
        <div>
         <h3 style="text-align: center;"> <form action="/<%= user._id %>/allcomments" method="post"> <button type="submit"> <u> All Comments </u></button></form>
         <form action="/<%= user._id %>/mycomments" method="post"> <button type="submit"> My Comments</button> </a> </h3></form>
     </div>
        <% for (let i in comments ){ %>
            <h3><%= comments[i].username %>
            wrote:
               <br> <%= comments[i].comment %> 
        <% } %>

    </body>
</html>