<!DOCTYPE html>
<html>
    <head>
        <title>Comment Section</title>
    </head>
    <body style="font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;">
       <form action="/login" mehtod="post"> <button type="submit"> Logout</button></form>
        <h2 style="text-align: center;"> Welcome <%= user.username %>!</h2> 
       <div>
           <p>
        <h3 style="text-align: center;"> <form action="/<%= user._id %>/allcomments" method="post"> <button type="submit"> All Comments </button></form>
        <form action="/<%= user._id %>/mycomments" method="post"> <button type="submit"><u> My Comments</u></button> </a> </h3></form>
            </p>
    </div>
    <div>
        <form action="/<%= user._id %>/newcomment" method ="post"> 
            <textarea name="comment" cols="40" rows="5"></textarea>
            <button type="submit">Add Comment</button>
        </form>
    </div>
        <% for (let i=0; i< user.comments.length; i++ ){ %>
            <h3 style="font-family: Roboto, sans-serif;"><p> <%= user.comments[i] %> 
            <form action="/<%= user._id %>/<%= i %>/delete" method="post">
            <button type="submit">Delete</button></form></p>
        <% } %>

    </body>
</html>